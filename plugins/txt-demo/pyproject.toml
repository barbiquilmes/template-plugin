[project]
name = "txt-demo"
version = "0.1.0"
description = "MCP server for txt-demo plugin"
requires-python = ">=3.10"
dependencies = ["mcp==1.9.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
